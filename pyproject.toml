[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dualv"
version = "0.1.0"
description = "Librería de autodiferenciación con números duales"
authors = [
    {name = "dver26"}
]
